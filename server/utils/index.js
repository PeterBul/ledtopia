import { getLocalDevices } from "./network.js";
import { sockets, setupConnections } from "./connections.js";

export { getLocalDevices, sockets, setupConnections };

export default { getLocalDevices, sockets, setupConnections };
